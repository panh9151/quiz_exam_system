import React from "react";
import Main from "../parts/QuizParts";
// import { useParams } from "react-router-dom";

const Quiz = () => {
    // let { quizId } = useParams();

    return (
        <Main />
    )
}

export default Quiz;